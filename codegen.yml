overwrite: true
schema: "lambda/schema.graphql"
documents: 'app/graphql/*.graphql'
generates:
  app/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
